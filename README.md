# customrange
A simple Custom Range Counter built using HTML, CSS, Bootstrap, and JavaScript. Users can enter starting and ending values to generate an automatic counter that updates every second. This project demonstrates DOM manipulation, event handling, input validation, and setInterval().
